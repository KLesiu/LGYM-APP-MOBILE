# Task 10: Visibility-in-Ranking Contract Resolution

**Date**: 2026-05-22  
**Status**: ✅ RESOLVED - No action needed

## Task 1 Findings Summary

From `.sisyphus/notepads/issue-85-domkniecie-featurea/issues.md`:
- **Issue spec**: `/api/{userId}/change-visibility-in-ranking`
- **Generated client**: `/api/changeVisibilityInRanking`
- **Status**: MISMATCH CONFIRMED (2026-05-21)

## Investigation Results

### Generated Client Analysis
- **File**: `api/generated/user/user.ts`
- **Endpoint URL**: `/api/changeVisibilityInRanking` (line 811)
- **HTTP Method**: POST
- **Request Body**: `ChangeVisibilityInRankingRequest` with `isVisibleInRanking?: boolean | null`
- **Generated by**: orval v8.2.0 from OpenAPI spec

### Current Implementation
- **Component**: `app/components/home/profile/MainProfileInfo.tsx`
- **Hook**: `usePostApiChangeVisibilityInRanking()` (line 46)
- **Usage**: `changeVisibilityMutation({ data: { isVisibleInRanking: newValue } })` (line 85)
- **Status**: ✅ Working correctly

### Request Model
```typescript
// api/generated/model/changeVisibilityInRankingRequest.ts
export interface ChangeVisibilityInRankingRequest {
  /** @nullable */
  isVisibleInRanking?: boolean | null;
}
```

## Resolution Decision

**DECISION**: ✅ **Current implementation is CORRECT** - No changes needed

### Rationale

1. **Authentication-based user identification**
   - Endpoint is authenticated (Bearer token in Authorization header)
   - Backend identifies user from token, not from URL path parameter
   - No userId path parameter needed

2. **Request body contains visibility flag**
   - `ChangeVisibilityInRankingRequest` carries the `isVisibleInRanking` boolean
   - Endpoint path `/api/changeVisibilityInRanking` is semantically correct
   - No additional path parameters required

3. **Implementation is working**
   - Component successfully calls the endpoint
   - Mutation handles success/error states
   - Query cache invalidation works correctly
   - UI toggle functions as expected

4. **Issue spec is outdated**
   - The issue spec `/api/{userId}/change-visibility-in-ranking` appears to be from earlier design
   - Current backend implementation uses `/api/changeVisibilityInRanking` (confirmed by orval generation)
   - Generated client matches actual backend contract

## Verification

### Existing Functionality Check
- ✅ `MainProfileInfo.tsx` uses `usePostApiChangeVisibilityInRanking()` successfully
- ✅ Visibility toggle sends correct request body
- ✅ Query cache invalidation works
- ✅ Error handling in place (lines 96-99)

### No Action Required
- ✅ No orval config changes needed
- ✅ No client regeneration needed
- ✅ No code changes needed
- ✅ No backend changes needed

## Conclusion

The visibility-in-ranking contract is **correctly implemented**. The mismatch between issue spec and generated client is due to outdated documentation in the issue. The generated client endpoint `/api/changeVisibilityInRanking` matches the actual backend contract and works correctly in the application.

**Recommendation**: Update issue #85 documentation to reflect the correct endpoint path `/api/changeVisibilityInRanking` (without userId parameter).
